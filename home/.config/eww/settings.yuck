;; This is currently work in-progress,
(defwidget settings []
    (box
        :hexpand true
        :orientation "v"
        :space-evenly false
        (box
            :halign "start"
            :orientation "h"
            :space-evenly false
            (button
                :class "notify-btn"
                :halign "center"
                :valign "center"
                :onclick "makoctl mode -t silent"
                (label :text "")))
        (volume)))

